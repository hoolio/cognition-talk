export default function SpeakerNotesPage() {
  return (
    <article className="prose">
      <h1>Speaker notes</h1>
      <p>
        Per-slide talk tracks will live in <code>content/speaker-notes/</code>, one
        markdown file per slide or section.
      </p>
    </article>
  );
}
